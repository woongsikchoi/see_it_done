{
  "//" : "Ensure these items are synced across projects",
  "scripts": {
    "yarn2" : "yarn --ignore-scripts",
    "yarnfast" : "yarn --ignore-scripts --offline",
    "clean" :"rmdir node_modules /s /q",
    "local": "npm install --ignore-scripts",
    "test": "npm run line && mocha \"./{,!(node_modules)/**/}*.runner.js\" --timeout 3000",
    "test_yarn": "npm run yarn2 && npm test && npm run fix",
    "qa" : "npm run coverage_test",
    "coverage_test": "npm run line && nyc mocha \"./{,!(node_modules)/**/}*.runner.js\"  --timeout 3000",
    "coverage_report": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "coverage": "npm run coverage_test && npm run coverage_report",
    "rules": "standard | snazzy",
    "fix": "standard --fix",
    "status": "npm run line && git log --since=2.weeks --pretty=format:\"%h - %an, %ar : %s\" && git status",
    "commit": "npm run fix && git add . && git commit -a -m \"config updates\"",
    "commitAll": "npm run yarn2 && npm run fix && npm run commit && npm test && npm run push",
    "commitFast": "npm run line && npm run commit && npm run push && git status",
    "pull": "npm run line && git pull && npm run yarn2 && npm test",
    "gitfix": "git rm -r --cached . && git add . && git commit -m \".gitignore fix\"",
    "patch": "npm version patch -m \"Patch version %s\"",
    "push": "echo Push and release patch to master... && npm run line && git push origin master",
    "prep": "npm run fix && npm test && npm run patch",
    "release": "npm run fix && npm test && npm run patch && start npm run push",
    "beta": "npm run commit && npm run release",
    "ncu": "ncu -u -l info --timeout 70000 && npm run line && npm run yarn2",
    "upgrade": "npm run ncu && npm run commit && npm run release && npm run line",
    "install": "yarn global add babel-eslint codecov mocha nyc snazzy standard",
    "install2" : "npm install -g npm@latest",
    "line" : "echo ----------------------------------------------- && cd && echo -----------------------------------------------",
    "updateAll": "",
    "yarn_fast" : "",
    "yarnforce" : "",
    "update": "",
    "sync" : ""
},
  "nyc": {
    "exclude": [
      "**/test.runner.js",
      "**/readline.js**"
    ]
  },
  "standard": {
    "env": [
      "mocha"
    ],
    "ignore": [
      "**/out/",
      "**/output/",
      "/lib/select2/",
      "/lib/ckeditor/",
      "tmp.js",
      "**/templates/",
      "**/dist/",
      "**/docs/"
    ]
  }
}
